spring:
  application:
    name: spring-rag-application

  datasource:
    url: jdbc:postgresql://localhost:5434/ragdb
    username: raguser
    password: ragpass

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:}
      chat:
        enabled: false
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: gpt-oss:20b
          temperature: 0.3
      embedding:
        options:
          model: qwen3-embedding:4b

  data:
    redis:
      host: localhost
      port: 6381

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    check-template-location: true

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  chunking:
    mode: semantic                # "fixed" 또는 "semantic"
    fixed:
      chunk-size: 1000
      overlap: 200
    semantic:
      buffer-size: 1
      breakpoint-percentile: 90
      min-chunk-size: 200
      max-chunk-size: 1500
  conversation:
    max-messages: 20
    ttl-minutes: 60
  search:
    expansion-count: 3
    rerank-candidates: 15
  rag:
    max-sub-queries: 3
    merge-analysis: true
  evaluation:
    sample-rate: 0.1
  guard:
    rate-limit-per-minute: 10
    admin-rate-limit-per-minute: 30
    max-input-length: 2000
  upload:
    avatar-dir: ${AVATAR_UPLOAD_DIR:./uploads/avatars}
    document-dir: ${DOCUMENT_UPLOAD_DIR:./uploads/documents}
  jwt:
    secret: ${JWT_SECRET:spring-rag-app-dev-secret-key-minimum-32-chars}
    access-token-expiry: 1800000
    refresh-token-expiry: 604800000
  tavily:
    api-key: ${TAVILY_API_KEY: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}
  model:
    ollama:
      default-base-url: http://localhost:11434

logging:
  level:
    pipeline.trace: INFO
    evaluation: INFO
    com.example.rag.generation: DEBUG
    com.example.rag.auth.JwtAuthenticationFilter: DEBUG

server:
  port: 8080
