# docker-compose-dev.yml에서 참조하는 환경변수
# 이 파일을 .env로 복사해서 값을 채워 사용하세요.
#   cp .env.example .env

# AI API Keys
ANTHROPIC_API_KEY=
TAVILY_API_KEY=

# JWT (개발용 기본값이 있으므로 비워둬도 됨)
# JWT_SECRET=spring-rag-app-dev-secret-key-minimum-32-chars
